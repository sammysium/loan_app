export const config = {
    default: {
        loan: {
            minimum: 10,
            maximum: 1000
        },
        currency: {
            name: "USD",
            symbol: "$",
            locale: "en-US"
        }
    }
    
}
