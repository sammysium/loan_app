export const config = {
    default: {
        loan: {
            minimum: 10
        },
        currency: {
            name: "USD",
            symbol: "$",
            locale: "en-US"
        }
    }
    
}
